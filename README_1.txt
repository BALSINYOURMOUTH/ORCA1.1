#INSTALL
	-extract the files
	-node.js
	-npm
#RUN 
	-`npm install` inside the project directory, follow the below steps for that
		+cmd[cd path/to/the/file]
		+cmd[npm install]
#SERVER
	-runthe server
		+cmd[node server/server.js]
#BROWSER
	-http://localhost:3000
